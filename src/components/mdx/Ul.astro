---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"ul"> {}

const { class: className, ...props } = Astro.props;
---

<ul class:list={["list-disc pl-[24px]", className]} {...props}><slot /></ul>
