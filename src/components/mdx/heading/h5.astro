---
import Heading from "./Heading.astro";

interface Props {
	class?: string;
	id?: string;
}
const { class: className, id } = Astro.props;
---

<Heading level={5} class={className} id={id}>
	<slot />
</Heading>
